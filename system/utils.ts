import { Err } from "./interfaces.ts";

const secretKey = "Key3227"

// formato do JWT : Header.Payload.Signature
function generateJWT(): [string, Err] {
   return
}

function validateJWT(): [boolean, Err] {
   return
}